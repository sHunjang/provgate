// ============================================================
// /problems (구 문제 목록) → /learn 리다이렉트
// ============================================================
// 이 페이지는 원래 문제 목록 UI를 직접 그렸지만,
// /learn이 완전히 동일한 기능(+트랙 구분)을 대체하게 되면서
// 더 이상 별도로 유지할 이유가 없어짐.
//
// 코드를 완전히 지우지 않고 리다이렉트로 남겨두는 이유:
// 예전에 외부 커뮤니티(OKKY 등)에 이 경로로 링크를 공유한 적이
// 있어서, 완전 삭제하면 그 링크들이 전부 404가 됨.
// redirect()는 Next.js가 서버에서 즉시 새 경로로 안내해주는
// 함수라, 이 페이지에 방문한 사람은 화면 깜빡임 없이
// 자동으로 /learn으로 이동함 (HTTP 307 리다이렉트).
import { redirect } from "next/navigation";

export default function ProblemsRedirectPage() {
    redirect("/learn");
}